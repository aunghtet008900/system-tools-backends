All:

  - Test returned filehandles from be_open_*() and be_write_*() in the correct
    manner (assign to $fh, test that, and assign to FILE if non-null).

* users-conf.in: 

  - Yellow pages support.
  - test commands' success and report otherwise.

* networking-conf.in:

  - Update the /etc/hosts entry for local host's IP. Needed for nmblookup.
